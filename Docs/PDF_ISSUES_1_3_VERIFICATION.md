# PDF issues 1-3 verification

This version replaces the former mock-facade ray network with an auditable
real-rooftop network generated by
`Scripts/SignalRayDemo/build_pdf_issues_1_3.py`.

## Issue 1: green rays were too bright

- Green color: `(0.0, 0.72, 0.08, 1.0)`
- Green glow intensity: `8.0`
- Green opacity: `0.78`
- Post-process bloom intensity: `0.45`

## Issue 2: sources were concentrated

- Source count: `8`
- Selection: farthest-point sampling over real Cesium rooftop hits
- Measured minimum XY source separation in the verified build: `24738.63 cm`
- Measured maximum XY source separation in the verified build: `129973.07 cm`
- Every source has a visible mast whose lower endpoint is its real rooftop hit.

## Issue 3: reflection points floated

- Ray count: `64`
- Real rooftop reflection count: `64`
- Artificial/fallback reflection count: `0`
- Collision component class for all 64 hits: `CesiumGltfPrimitiveComponent`
- Lowest accepted roof-normal Z in the verified build: `0.858701`
- Distinct rooftop probe count: `73`
- Rays per source: `10, 3, 10, 10, 10, 9, 9, 3`

The script traces the complex Cesium triangle mesh directly with
`line_trace_component`. It rejects a path if the hit is absent, outside the
urban bounds, below the minimum building elevation, or not sufficiently
upward-facing. There is no estimated plane and no fallback point.

The full per-ray audit record is regenerated at runtime in:

`Saved/SignalRayDemo/pdf_issues_1_3_real_collision.json`
