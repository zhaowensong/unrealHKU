# TelecomTwin 第三版修正版独立补丁使用说明

## 先说结论

对方可以继续使用已经下载好的**第一版完整压缩包**，不需要重新下载整个项目，也不需要安装或补打第二版补丁。

正确的交付关系是：

```text
第一版完整项目
  + 对方自己安装的 PowerShell 7
  + 第三版修正版“双层信道修复 + 人物关联线恢复”独立补丁
  = 可演示版本
```

第二版只处理 Windows PowerShell 5.1 的脚本兼容问题。对方已经通过安装 PowerShell 7 自行解决，因此第三版补丁**不得覆盖第二版改过的启动脚本**，也不要求对方先安装第二版。

## 三个版本分别是什么

| 版本 | 用途 | 对方是否需要 |
|---|---|---:|
| 第一版 | 完整项目、城市、人物、插件、素材和一键启动入口 | 已经有，保留 |
| 第二版 | 兼容 Windows PowerShell 5.1 | 不需要 |
| 第三版修正版 | 移除晚加载的旧悬浮信道层，并恢复蓝色/灰色人物—基站关联线 | 需要覆盖 |

如果对方已经覆盖过最初的第三版文件，也不需要恢复第一版；直接再覆盖第三版修正版即可。修正版包含同一组运行文件的新版本，并继续完全排除第二版 PowerShell 文件。

## 第三版补丁必须包含的文件

第三版压缩包应保持下面的相对目录结构：

```text
TelecomTwin_第三版修正版_双层信道及人物关联线/
├─ Plugins/
│  └─ OpenMassCrowd/
│     ├─ Binaries/
│     │  └─ Win64/
│     │     ├─ UnrealEditor-OpenMassCrowd.dll
│     │     └─ UnrealEditor.modules
│     └─ Source/
│        └─ OpenMassCrowd/
│           ├─ Private/
│           │  └─ OpenMassCrowdSpawner.cpp
│           └─ Public/
│              └─ OpenMassCrowdSpawner.h
└─ Scripts/
   └─ OpenMassCrowd/
      ├─ orchestrate_investor_delivery_demo.py
      ├─ verify_investor_delivery_demo_runtime.py
      └─ capture_investor_signal_alignment_evidence.py
```

其中 `UnrealEditor-OpenMassCrowd.dll` 是运行时真正生效的核心文件，不能只发送 C++ 源码。第一版启动器只负责打开项目，不会自动把新源码重新编译成 DLL；如果漏掉新 DLL，对方启动后仍会运行第一版旧逻辑，信道悬空问题不会消失。

`UnrealEditor.modules` 应和 DLL 一起发送，以保持 UE 5.7 模块信息一致。PDB 调试文件体积很大，正常演示不需要发送。

## 第三版明确不能包含的文件

以下文件属于第二版 PowerShell 兼容修改，本次第三版独立补丁不要携带、不要覆盖：

```text
启动TelecomTwin演示.bat
Scripts/OpenMassCrowd/start_investor_delivery_demo.ps1
Scripts/OpenMassCrowd/launch_telecomtwin_citysample.ps1
START_HERE.md
```

这样做不会撤销对方现有环境，也不会把第二版补丁夹带过去。对方继续使用第一版的 BAT/PowerShell 脚本，由已经安装的 PowerShell 7 执行即可。

## 对方的安装步骤

1. 完全关闭 Unreal Editor 和当前演示窗口。
2. 找到第一版解压后的 `TelecomTwin` 项目根目录；该目录中应能看到 `TelecomTwin.uproject`。
3. 先复制一份该目录作为备份，例如 `TelecomTwin_第一版备份`。
4. 打开第三版补丁压缩包，把其中的 `Plugins` 和 `Scripts` 两个目录复制到 `TelecomTwin` 根目录。
5. Windows 询问时选择“替换目标中的文件”。
6. 不要删除第一版中的其他素材，不要把补丁解压成 `TelecomTwin/TelecomTwin/...` 双层目录。
7. 覆盖完成后，仍然双击第一版原有的 `启动TelecomTwin演示.bat`。

正确目录示例：

```text
TelecomTwin/
├─ TelecomTwin.uproject
├─ 启动TelecomTwin演示.bat
├─ Plugins/
│  └─ OpenMassCrowd/
│     └─ Binaries/Win64/UnrealEditor-OpenMassCrowd.dll
└─ Scripts/
   └─ OpenMassCrowd/orchestrate_investor_delivery_demo.py
```

## 为什么可以成功

从版本差异看，第二版只改了两个 PowerShell 启动脚本和三份说明文档；第三版的运行时修复改动位于 OpenMassCrowd 的 C++ 模块与 Python 验收脚本中，两者没有文件覆盖冲突。

因此，把第三版运行文件直接覆盖到第一版上，在文件层面是可行的。对方已经安装 PowerShell 7，也满足第一版启动脚本的解释器要求。

成功还依赖以下条件：

- 对方使用的确实是原来的第一版完整项目，而不是缺文件的手工拷贝。
- 对方安装的是相同的 Unreal Engine 5.7。
- 覆盖补丁时 UE 已关闭，否则旧 DLL 可能被进程占用而无法替换。
- 第三版压缩包包含新编译的 DLL，而不只是源码和文档。
- 补丁解压后的相对目录没有多套一层文件夹。

满足这些条件后，对方成功启动的概率很高。最大的实际风险不是“没安装第二版”，而是 DLL 没有成功覆盖、目录套错层，或者覆盖时 UE 仍在运行。

## 现场验收方法

启动进入人物移动演示后，按下面顺序检查：

1. 场景中正常出现人物并持续行走。
2. 绿色、黄色、橙色、红色四类信道仍然存在。
3. 建筑之间的信道落点与未运行人物时一致，不能出现整体抬高的悬空线、屋顶上方的竖直假信标、圆环或覆盖圈。
4. 选中人物到服务基站显示蓝色实线，其他抽样人物显示灰色虚线；这些是业务关联线，不是新增反射层。
5. 所有人物关联线的基站端点必须落在实时 Cesium 屋顶点上方 4 cm，不能连接到高空假节点。
6. 按 `Esc` 退出 PIE，再次进入人物演示，信道位置仍应保持一致。
7. 如果运行时仍悬空或没有蓝/灰关联线，先检查下面的 DLL 是否确实被第三版覆盖：

```text
Plugins/OpenMassCrowd/Binaries/Win64/UnrealEditor-OpenMassCrowd.dll
```

修正版验收还必须显示：已加载的旧 `SIG_RaySegment_* / SIG_Node_* / SIG_Source_Main` 对象可见数为 0。仅看到 30 + 1920 和 1950 个批次实例还不够，因为最初第三版正是遗漏了这组 World Partition 晚加载对象。

当前修复版本的本机编译时间为 `2026-08-11 22:34:27`，文件大小为 `1,106,944` 字节，SHA-256 为：

```text
456BF9BD88CDF462B90B64E4777B00CD67718C6C4F6D0EDB52AF7679B80E464F
```

接收方覆盖后可以再次计算并比对，避免网盘或解压过程造成文件错误。

## 出问题时如何回滚

关闭 UE，把当前 `TelecomTwin` 目录移走，再把之前保存的 `TelecomTwin_第一版备份` 改回 `TelecomTwin` 即可。第三版补丁不修改关卡资产和人物素材，因此回滚只需要恢复原目录，无须重新安装 Unreal Engine 或 PowerShell 7。

## 给接收方的一句话说明

> 请关闭 UE，备份原 TelecomTwin 文件夹，然后把第三版补丁中的 Plugins 和 Scripts 覆盖到第一版项目根目录。不要安装第二版补丁；覆盖完成后仍双击原来的“启动TelecomTwin演示.bat”。
